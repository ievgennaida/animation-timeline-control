export enum TimelineDraggableType {
  /**
   * Drag keyframe
   */
  keyframe = 'keyframe',
  /**
   * Drag keyframes stripe.
   */
  keyframes = 'keyframes',
  /**
   * Drag time indicator
   */
  timeline = 'timeline',
}

import React from 'react';
import { render, screen } from '@testing-library/react';
import ReactTimelineControl from './ReactTimelineControl';

test('renders react timeline control component', () => {
  render(<ReactTimelineControl />);
});

import React from 'react';
import { render, screen } from '@testing-library/react';
import ReactTimeline from './ReactTimeline';

test('renders react timeline control component', () => {
  render(<ReactTimeline />);
});
